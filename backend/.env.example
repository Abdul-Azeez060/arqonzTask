PORT=4000
# Set your deployed frontend origin for CORS, or leave empty to allow all
FRONTEND_ORIGIN=http://localhost:5173
# Supabase client environment for server if needed
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
